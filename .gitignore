.DS_Store
Thumbs.db
node_modules/
originals/
__pycache__/
*.pyc
.claude/settings.local.json

# Master session list (session order/topic -> slug, all in one place).
# Generated by scripts/build_decks.py -- kept local-only, never published
# or committed, so the repo itself never lists every session together.
/index.html
